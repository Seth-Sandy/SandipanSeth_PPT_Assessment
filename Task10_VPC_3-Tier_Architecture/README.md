# VPC 3-Tier Architecture
A secure VPC architecture that uses public subnets for internet access and private subnets for app/data tiers, controlled by route tables and a NAT Gateway to allow one-way outgoing traffic while blocking direct external entry.
