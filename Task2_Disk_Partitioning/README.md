# Disk Partitioning
Attaches a new EBS volume to a Windows instance, initializes it using Disk Management, and formats it with NTFS for immediate storage use.
Partitioning, formatting, and mounting a new EBS volume, then configuring it to automatically mount on boot and setting permissions for user access.
